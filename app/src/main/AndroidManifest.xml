<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Cleartext is permitted app-wide because BlueBubbles servers are commonly plain-HTTP on a LAN
      at an address the user types in; TLS-proxied setups still work.

      This CANNOT be narrowed with a network-security-config: <domain> accepts hostnames and
      individual IP literals only, not CIDR ranges, so "permit cleartext on RFC1918 only" is not
      expressible. Since the server address is arbitrary and runtime-supplied, there is no static
      allowlist to write. The real mitigation is a runtime check — the password travels as a
      ?guid= query parameter on every request, so plaintext HTTP to a *public* host leaks it — and
      that is tracked separately, not solved by this attribute.

      Backup is off deliberately. Credentials live in EncryptedSharedPreferences, whose master key
      is held in the Android Keystore and is NOT part of a backup; a restored install would carry an
      undecryptable blob and fail to read its own credentials. The message database is also a full
      copy of the user's conversations, which is not something a self-hosted client should ship to
      a third-party cloud by default. Nothing is lost: the app's entire state re-derives from the
      server after onboarding.
    -->
    <application
        android:name=".RebubbleApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rebubble"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Rebubble">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- zxing-embedded CaptureActivity for QR onboarding -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

        <!-- Runtime-initialized FCM (no google-services.json). Bound to the default FirebaseApp. -->
        <service
            android:name=".notifications.PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".notifications.ReplyReceiver"
            android:exported="false" />

        <!-- Disable default WorkManager initializer; RebubbleApplication supplies HiltWorkerFactory. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
